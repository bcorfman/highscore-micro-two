"https://{default}/docs":
  type: upstream
  upstream: "highscore:http"

"https://{default}/clear_scores":
  type: upstream
  upstream: "highscore:http"

"https://{default}/high_scores":
  type: upstream
  upstream: "highscore:http"

"https://{default}/add_score":
  type: upstream
  upstream: "highscore:http"
